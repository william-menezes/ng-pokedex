import { CalculatePercentagePipe } from './calculate-percentage.pipe';

describe('CalculatePercentagePipe', () => {
  it('create an instance', () => {
    const pipe = new CalculatePercentagePipe();
    expect(pipe).toBeTruthy();
  });
});
