import { MeasuresConverterPipe } from './measures-converter.pipe';

describe('MeasuresConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new MeasuresConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
